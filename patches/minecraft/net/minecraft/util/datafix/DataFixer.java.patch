--- before/net/minecraft/util/datafix/DataFixer.java
+++ after/net/minecraft/util/datafix/DataFixer.java
@@ -45,7 +45,7 @@
 
         if (list != null)
         {
-            for (int i = 0; i < list.size(); i++)
+            for (int i = 0; i < list.size(); ++i)
             {
                 IFixableData ifixabledata = list.get(i);
 
@@ -65,7 +65,7 @@
 
         if (list != null)
         {
-            for (int i = 0; i < list.size(); i++)
+            for (int i = 0; i < list.size(); ++i)
             {
                 compound = list.get(i).process(this, compound, versionIn);
             }
@@ -97,7 +97,7 @@
         {
             if (!list.isEmpty() && Util.getLastElement(list).getFixVersion() > i)
             {
-                for (int j = 0; j < list.size(); j++)
+                for (int j = 0; j < list.size(); ++j)
                 {
                     if (list.get(j).getFixVersion() > i)
                     {
