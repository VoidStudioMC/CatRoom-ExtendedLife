--- before/net/minecraft/advancements/critereon/PlacedBlockTrigger.java
+++ after/net/minecraft/advancements/critereon/PlacedBlockTrigger.java
@@ -107,7 +107,7 @@
                 if (iproperty == null)
                 {
                     throw new JsonSyntaxException(
-                        "Unknown block state property '" + entry.getKey() + "' for block '" + Block.REGISTRY.getNameForObject(block) + "'"
+                        "Unknown block state property '" + (String)entry.getKey() + "' for block '" + Block.REGISTRY.getNameForObject(block) + "'"
                     );
                 }
 
@@ -117,7 +117,13 @@
                 if (!optional.isPresent())
                 {
                     throw new JsonSyntaxException(
-                        "Invalid block state value '" + s + "' for property '" + entry.getKey() + "' on block '" + Block.REGISTRY.getNameForObject(block) + "'"
+                        "Invalid block state value '"
+                        + s
+                        + "' for property '"
+                        + (String)entry.getKey()
+                        + "' on block '"
+                        + Block.REGISTRY.getNameForObject(block)
+                        + "'"
                     );
                 }
 
@@ -181,10 +187,15 @@
                         }
                     }
 
-                    return !this.location
-                           .test(world, (float)pos.getX(), (float)pos.getY(), (float)pos.getZ())
-                           ? false
-                           : this.item.test(item);
+                    if (!this.location
+                            .test(world, (float)pos.getX(), (float)pos.getY(), (float)pos.getZ()))
+                    {
+                        return false;
+                    }
+                    else
+                    {
+                        return this.item.test(item);
+                    }
                 }
             }
         }
