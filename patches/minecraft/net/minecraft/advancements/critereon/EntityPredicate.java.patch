--- before/net/minecraft/advancements/critereon/EntityPredicate.java
+++ after/net/minecraft/advancements/critereon/EntityPredicate.java
@@ -67,9 +67,13 @@
         {
             return false;
         }
+        else if (!this.effects.test(entity))
+        {
+            return false;
+        }
         else
         {
-            return !this.effects.test(entity) ? false : this.nbt.test(entity);
+            return this.nbt.test(entity);
         }
     }
 
