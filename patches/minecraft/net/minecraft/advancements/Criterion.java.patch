--- before/net/minecraft/advancements/Criterion.java
+++ after/net/minecraft/advancements/Criterion.java
@@ -70,7 +70,7 @@
         Map<String, Criterion> map = Maps.newHashMap();
         int i = bus.readVarInt();
 
-        for (int j = 0; j < i; j++)
+        for (int j = 0; j < i; ++j)
         {
             map.put(bus.readString(32767), criterionFromNetwork(bus));
         }
