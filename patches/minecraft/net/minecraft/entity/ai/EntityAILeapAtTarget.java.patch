--- before/net/minecraft/entity/ai/EntityAILeapAtTarget.java
+++ after/net/minecraft/entity/ai/EntityAILeapAtTarget.java
@@ -34,9 +34,13 @@
             {
                 return false;
             }
+            else if (!this.leaper.onGround)
+            {
+                return false;
+            }
             else
             {
-                return !this.leaper.onGround ? false : this.leaper.getRNG().nextInt(5) == 0;
+                return this.leaper.getRNG().nextInt(5) == 0;
             }
         }
     }
@@ -56,8 +60,8 @@
 
         if ((double)f >= 1.0E-4)
         {
-            this.leaper.motionX = this.leaper.motionX + d0 / (double)f * 0.5 * 0.8F + this.leaper.motionX * 0.2F;
-            this.leaper.motionZ = this.leaper.motionZ + d1 / (double)f * 0.5 * 0.8F + this.leaper.motionZ * 0.2F;
+            this.leaper.motionX += d0 / (double)f * 0.5 * 0.8F + this.leaper.motionX * 0.2F;
+            this.leaper.motionZ += d1 / (double)f * 0.5 * 0.8F + this.leaper.motionZ * 0.2F;
         }
 
         this.leaper.motionY = (double)this.leapMotionY;
