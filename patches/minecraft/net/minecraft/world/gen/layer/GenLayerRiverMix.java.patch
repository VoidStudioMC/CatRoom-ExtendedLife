--- before/net/minecraft/world/gen/layer/GenLayerRiverMix.java
+++ after/net/minecraft/world/gen/layer/GenLayerRiverMix.java
@@ -30,7 +30,7 @@
         int[] aint1 = this.riverPatternGeneratorChain.getInts(areaX, areaY, areaWidth, areaHeight);
         int[] aint2 = IntCache.getIntCache(areaWidth * areaHeight);
 
-        for (int i = 0; i < areaWidth * areaHeight; i++)
+        for (int i = 0; i < areaWidth * areaHeight; ++i)
         {
             if (aint[i] == Biome.getIdForBiome(Biomes.OCEAN) || aint[i] == Biome.getIdForBiome(Biomes.DEEP_OCEAN))
             {
