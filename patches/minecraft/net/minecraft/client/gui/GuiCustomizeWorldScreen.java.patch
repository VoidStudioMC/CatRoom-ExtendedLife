--- before/net/minecraft/client/gui/GuiCustomizeWorldScreen.java
+++ after/net/minecraft/client/gui/GuiCustomizeWorldScreen.java
@@ -411,7 +411,7 @@
             aguipagebuttonlist$guilistentry3
         );
 
-        for (int i = 0; i < 4; i++)
+        for (int i = 0; i < 4; ++i)
         {
             this.pageNames[i] = I18n.format("createWorld.customize.custom.page" + i);
         }
@@ -960,7 +960,7 @@
                     break;
                 case 301:
 
-                    for (int i = 0; i < this.list.getSize(); i++)
+                    for (int i = 0; i < this.list.getSize(); ++i)
                     {
                         GuiPageButtonList.GuiEntry guipagebuttonlist$guientry = this.list.getListEntry(i);
                         Gui gui = guipagebuttonlist$guientry.getComponent1();
