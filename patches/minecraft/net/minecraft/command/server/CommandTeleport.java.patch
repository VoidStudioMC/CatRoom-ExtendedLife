--- before/net/minecraft/command/server/CommandTeleport.java
+++ after/net/minecraft/command/server/CommandTeleport.java
@@ -61,7 +61,7 @@
                 CommandBase.CoordinateArg commandbase$coordinatearg3 = parseCoordinate(
                             args.length > j ? (double)entity1.rotationYaw : (double)entity.rotationYaw, args.length > j ? args[j] : "~", false
                         );
-                j++;
+                ++j;
                 CommandBase.CoordinateArg commandbase$coordinatearg4 = parseCoordinate(
                             args.length > j ? (double)entity1.rotationPitch : (double)entity.rotationPitch, args.length > j ? args[j] : "~", false
                         );
